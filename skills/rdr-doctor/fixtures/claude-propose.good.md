Outcome: Propose complete — RDR 0042 has a chosen approach and a Recommendation an implementer could act on.
Gate: PASS
RDR delta: chose the streaming walker over the buffer-all rewrite (decisive: 0038's OOM triage); rejected the in-memory variant and the third-party parser; new Pending A3 (back-pressure bound) and A4 (partial-flush semantics).
Deviations: none
Continue check: contract is named and testable; A3/A4 are verification-shaped, not design-open — Resolve can close them against source.
Next: /rdr-resolve 0042 (Claude) / $rdr-resolve 0042 (Codex) — or /rdr-status 0042 to re-orient
