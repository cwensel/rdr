# Stage 7.1 Cluster Reconcile — cluster `0021-0022-0023` (iteration 2)

The re-run that admitted 0023. Both directories persist: this one is the
later iteration of `0021-0022`, so 0021 and 0022 match twice and the
overlap is nested, not ambiguous.
