package cli

// NOT a test file. It carries the literal ".dml.sql" and a line shaped
// like a record-named test; neither may be read, because the glob names
// only *_test.go — the read-count test pins that this file stays closed.

const sidecarExt = ".dml.sql"

func TestCorpus0103_REQ9_Decoy() {}
